# Ceramics by Naama

Before making any change, read [README.md](README.md) — it contains the full project context: architecture (Google Drive as CMS), the filename convention, why the site is a single index.html, the API key referrer restriction (Drive returns 403 on localhost — expected), the mock-data testing workflow, and the working rules (behavior-preserving fixes OK; interface/UX changes must be proposed as a numbered list for approval first).
